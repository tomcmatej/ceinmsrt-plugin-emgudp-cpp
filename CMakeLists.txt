CMAKE_MINIMUM_REQUIRED (VERSION 3.5) 
PROJECT(EMG_UDP_Simulink)

IF(WIN32)	
	ADD_DEFINITIONS(-DWIN32)
	ADD_DEFINITIONS(-DWINDOWS)
	SET(CMAKE_C_FLAGS "ws2_32.lib")
	SET(WINLIB ws2_32)
	SET(WINMM winmm)	
ENDIF(WIN32)

INCLUDE_DIRECTORIES(
	include
)
add_subdirectory(src)
